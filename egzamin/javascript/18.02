<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<p id="wynik"></p>
<script>
 var wypisz=document.getElementById("wynik");
 var x=0;
 var a=4;
 var b=6;
 var c=3;
 var d=7;
 var liczba=-100;
 var wiek=10;
 
 if (b == 0)
 {
  wypisz.innerHTML = "Nie można dzielić przez 0!";
 }
 else
  {
  var wynik = a / b;
  wypisz.innerHTML = "Wynik dzielenia wynosi: " + wynik;
  }

  wypisz.innerHTML+="<br>";

 if(b == 0 || d == 0)
 {
    wypisz.innerHTML += "Nie można dzielić przez 0!";
 }
  
 else
 {
  var wynik = a / b + c / d;
  wypisz.innerHTML += "Wynik dzielenia wynosi: " + wynik;
 }
 
  wypisz.innerHTML+="<br>";

  if((b-4) == 0)
 {
    wypisz.innerHTML += "Nie można dzielić przez 0!";
 }
  
 else
 {
  var wynik = a + b / b - 4;
  wypisz.innerHTML += "Wynik dzielenia wynosi: " + wynik;
 }
 
  wypisz.innerHTML+="<br>";

  if(a % 2 == 0)
 {
    wypisz.innerHTML += "Liczba jest parzysta" ;
 }
  
 else
 {
  wypisz.innerHTML += "Liczba jest nieparzysta";
 }

 wypisz.innerHTML+="<br>"
  
 if(a % b == 0)
 {
   wypisz.innerHTML+="Liczba a jest podzielna przez b i wynosi: " +wynik;
 }
 
 else
 {
   wypisz.innerHTML+="Liczba a nie jest podzielna przez b";
 }

 wypisz.innerHTML+="<br>"
  
 if(liczba > 0)
 {
   wypisz.innerHTML+="Liczba jest dodatnia";
 }
 
 else if(liczba < 0)
 {
   wypisz.innerHTML+="liczba jest ujemna";
 }

 else if(liczba == 0)
 {
   wypisz.innerHTML+="liczba jest rowna 0";
 }

 wypisz.innerHTML+="<br>";

 
 if(wiek < 11)
 {
   wypisz.innerHTML+="Dziecko";
 }
 
 else if(wiek >= 11 && wiek <=17)
 {
   wypisz.innerHTML+="Nastolatek";
 }

 else if(wiek >= 18)
 {
   wypisz.innerHTML+="Dorosly";
 }














</script>

    
</body>
</html>